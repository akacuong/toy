<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Reset some default browser styles */
        body {
            margin: 0;
            padding: 0;
        }

        /* Custom Container Styles */
        .custom-container {
            max-width: 800px;
            margin: 20px auto; /* Center the container with some top margin */
            padding: 20px; /* Add padding for a cleaner look */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Add a subtle box shadow */
            border-radius: 8px; /* Add border-radius for rounded corners */
            background-color: #fff; /* White background */
        }

        /* Custom Row Styles */
        .custom-row {
            display: flex;
            justify-content: space-between;
            align-items: center; /* Align items vertically in the center */
            margin-top: 20px; /* Add margin at the top of the row */
        }

        /* Product Image Column Styles */
        .custom-col-md-3 {
            flex: 0 0 calc(50% - 10px); /* Adjusted to include a 20px gap */
            max-width: calc(50% - 10px); /* Adjusted to include a 20px gap */
            margin-right: 20px; /* Added margin for the gap */
        }

        /* Product Information Column Styles */
        .custom-col-md-2 {
            flex: 0 0 calc(50% - 10px); /* Adjusted to include a 20px gap */
            max-width: calc(50% - 10px); /* Adjusted to include a 20px gap */
            text-align: left;
        }

        /* Product Image Styles */
        .custom-product-image {
            width: 200px;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Back to Products Button Styles */
        .custom-btn-back {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            text-decoration: none;
            color: #fff;
            background-color: #007bff;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .custom-btn-back:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
   <div class="custom-container">
      <div class="custom-row">
        <!-- Product Image Column -->
        <div class="custom-col-md-4">
          <!-- Display the product image -->
          <img src="{{ product.image }}" alt="{{ product.name }}" class="custom-product-image">
        </div>

        <!-- Product Information Column -->
        <div class="custom-col-md-2 custom-text-center">
          <h1 class="mb-4">{{ product.name }}</h1>
          <p><strong>Category:</strong> {{ product.category.name }}</p>
          <p><strong>Price:</strong> ${{ product.price }}</p>
          <!-- Back to Products button -->
          <a href="/" class="btn btn-primary custom-btn-back">Back to Products</a>
        </div>
      </div>
  </div>
</body>
</html>
